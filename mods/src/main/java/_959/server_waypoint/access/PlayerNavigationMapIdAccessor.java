package _959.server_waypoint.access;

/** Access to the navigation map ID carried by a server player. */
public interface PlayerNavigationMapIdAccessor {
    int sw$getNavigationMapId();

    void sw$setNavigationMapId(int mapId);
}
