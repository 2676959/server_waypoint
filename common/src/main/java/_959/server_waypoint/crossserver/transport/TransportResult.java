package _959.server_waypoint.crossserver.transport;

/** Expected lifecycle outcomes. Diagnostics must not carry keys or platform exceptions. */
public enum TransportResult {
    SUCCESS,
    DISABLED,
    INVALID_CONFIGURATION,
    UNAVAILABLE
}
